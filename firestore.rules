rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients never access Firestore directly. All traffic goes through
    // the authenticated Next.js server using a service account.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
